"""
int(): Converts a value to an integer, can handle base conversions, e.g., int('1010', 2) → 10.  
bin(): Converts an integer to a binary string, e.g., bin(10) → '0b1010'.  
oct(): Converts an integer to an octal string, e.g., oct(10) → '0o12'.  
hex(): Converts an integer to a hexadecimal string, e.g., hex(255) → '0xff'.  
float(): Converts a value to a floating-point number, e.g., float('123') → 123.0.  
ord(): Converts a character to its Unicode integer, e.g., ord('A') → 65.  
chr(): Converts an integer to its corresponding Unicode character, e.g., chr(65) → 'A'.  
format(): Converts a number to a specified format (binary, octal, hex), e.g., format(255, 'b') → '11111111'.  
str(): Converts a value to a string, e.g., str(123) → '123'.  
round(): Rounds a floating-point number, e.g., round(3.14159, 2) → 3.14.  
"""

